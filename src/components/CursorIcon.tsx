import { FunctionComponent } from "react";
import styles from "./CursorIcon.module.css";
const CursorIcon: FunctionComponent = () => {
  return <div className={styles.groupChild} />;
};

export default CursorIcon;
